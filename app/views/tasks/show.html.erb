<h1>Show Task Page</h1>

<p>
  <strong><%= Task.human_attribute_name(:title) %>:</strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:content) %>:</strong>
  <%= @task.content %>
</p>

<%= link_to t('common.update'), @task %> 
|
<%= link_to t('common.back'), tasks_path %>